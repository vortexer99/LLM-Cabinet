# LLM Cabinet v0.5 → v0.6 手测清单

> 测试范围：v0.5.0 发布后到 HEAD 的所有功能改动
> 版本：0.6.0（待发布）

---

## 一、文件来源标记 + 折叠视图（task #30 / #04）

### 1.1 文件来源标记
- [ ] **导入文件**：拖入图片/PDF/视频，确认 files.origin = 'user'
- [ ] **生成封面**：对 PDF/视频使用「设为封面 → 截取画面」，确认生成的文件 origin = 'generated'
- [ ] **来源筛选**：文件表左上角 toggle（👤/🌐）存在时，切换到「仅用户文件」确认 generated 文件被隐藏

### 1.2 二态视图
- [ ] 有 generated 文件时，显示 👤/🌐 toggle 按钮
- [ ] 无 generated 文件时，toggle 按钮自动隐藏
- [ ] 切换 filter 后，文件列表正确过滤
- [ ] 切换项目后，filter 状态按项目独立记忆

---

## 二、项目列表多选（task #25）

### 2.1 多选操作
- [ ] **Ctrl + 点击**：选中多个不连续项目
- [ ] **Shift + 点击**：选中连续范围
- [ ] **全选**：Ctrl+A 选中所有项目
- [ ] **取消选中**：点击空白区域取消选择

### 2.2 批量操作
- [ ] 多选后右键菜单显示「批量删除」选项
- [ ] 批量删除显示确认对话框，包含项目数量和文件统计
- [ ] 批量删除后，项目正确从列表移除

---

## 三、导出增强（task #28 T1/T2）

### 3.1 导出选项扩展
- [ ] 单项目导出：右键菜单 / 工具栏导出
- [ ] 导出模式选择：「导出为独立包」/「仅导出项目元数据」
- [ ] 导出格式选择：「目录形式」/「ZIP 打包」
- [ ] 目录结构选择：「保留目录结构」/「拍平到 files/」
- [ ] 内容选项：包含 README.md、LLM 任务历史
- [ ] 封面图始终被复制（即使不勾选「复制链接文件」）

### 3.2 批量导出
- [ ] 多选项目后点击导出，进入批量导出模式
- [ ] 批量导出对话框显示项目列表（含文件数）
- [ ] 空项目（0 文件）灰显且默认不勾选
- [ ] 选择导出位置后，进度条显示导出进度
- [ ] 导出完成，每个项目在目标目录下有独立子文件夹

### 3.3 导出包结构
- [ ] files.json 包含 subfolder、is_cover、origin 字段（schema @3）
- [ ] 拍平结构下 files.json 的 subfolder 字段正确

---

## 四、导入增强（task #28 T3）

### 4.1 目录导入
- [ ] 拖入普通文件夹，创建新项目
- [ ] 拖入包含 project.json 的导出包，元数据正确恢复

### 4.2 ZIP 包导入
- [ ] 菜单「工具 → 导入项目包...」可选择 ZIP 文件
- [ ] 拖入 ZIP 文件，弹出选项：「导入项目包」/「解压为文件夹」
- [ ] 选择「导入项目包」，ZIP 被解压并走正常导入流程
- [ ] 导入完成后临时目录被清理

### 4.3 元数据恢复
- [ ] 封面图正确还原（cover_file_id 正确映射到新 file id）
- [ ] 拍平结构的目录树正确还原（subfolder 从 files.json 读取）
- [ ] 文件来源标记正确还原（origin: user/generated）

---

## 五、文件存储位置管理（task #29）

### 5.1 链接转仓储（T1）
- [ ] 选中 🔗 链接文件，右键「📦 转为仓储文件」
- [ ] 确认对话框显示待转换文件列表
- [ ] 原外部文件保留（复制语义）
- [ ] 转换后文件 is_relative = True，显示 📦 仓储

### 5.2 移动文件（T2）
- [ ] 选中文件，右键「📂 移动文件到...」
- [ ] 选择目标目录
- [ ] 文件物理移动到新位置
- [ ] 数据库 path 更新为新绝对路径，is_relative = False

### 5.3 重关联到外部文件（T3a）
- [ ] 选中 missing 标记的文件，右键「🔧 重关联到外部文件...」
- [ ] 选择外部文件目录
- [ ] 按文件名自动匹配，关联成功后 clear missing 标记
- [ ] 匹配失败的文件显示在提示中

### 5.4 替换链接目标（T3b）
- [ ] 单选 🔗 链接文件，右键「🔗 替换链接目标...」
- [ ] 选择新文件
- [ ] path 更新为新文件路径

---

## 六、文件视图增强（task #31a / #31b）

### 6.1 新增列
- [ ] 文件表新增「大小」列，显示物理文件大小（KB/MB/GB）
- [ ] 文件表新增「添加时间」列，显示文件添加时间
- [ ] 物理文件不存在时，大小列显示「—」

### 6.2 视图模式切换
- [ ] 文件表左上角显示「🌲」按钮（树形视图）
- [ ] 点击按钮切换到「📋」（扁平视图），图标变化
- [ ] 切换视图后，文件列表刷新显示正确格式
- [ ] 视图模式按项目独立记忆

### 6.3 扁平视图排序
- [ ] 扁平视图中，点击列头可排序
- [ ] 排序后显示排序箭头
- [ ] 切换项目再切回，排序状态保持

### 6.4 新建文件夹（T3）
- [ ] 文件表空白处右键，显示「📁 新建文件夹」选项
- [ ] 输入文件夹名称，确认后创建成功
- [ ] 目录节点右键，也可在该目录下新建子文件夹

### 6.5 重命名（T4）
- [ ] 选中文件，右键显示「✏️ 重命名」选项
- [ ] 弹出输入框，可修改文件说明（label）
- [ ] 确认后，文件表更新显示新名称

---

## 七、交互细节检查

### 7.1 菜单入口
- [ ] 「工具」菜单有「📥 导入项目包...」选项（Ctrl+I 快捷键）
- [ ] 导入/导出不冲突，无异常

### 7.2 拖放
- [ ] 拖入 ZIP 提示选择导入模式
- [ ] 拖入多个文件夹，提示批量导入选项
- [ ] 拖入空文件夹，创建 0 文件项目

### 7.3 独立窗口
- [ ] 文件表「⇱」按钮可弹出独立窗口
- [ ] 独立窗口中所有功能正常（右键菜单、添加文件等）
- [ ] 关闭窗口后文件表收回主窗口

### 7.4 一致性检查
- [ ] 工具 → 检查库一致性 功能正常
- [ ] missing 文件被正确标记
- [ ] missing 文件可使用「重关联」功能修复

---

## 八、边界情况

- [ ] 0 文件项目可正常创建、导出、删除
- [ ] 超大文件（>1GB）的大小显示正确
- [ ] 深层嵌套目录（>5层）导入后结构正确
- [ ] 导出到已有同名目录，有冲突处理
- [ ] 移动文件跨盘，进度正常

---

## 测试通过标准

- [ ] 所有勾选项测试通过
- [ ] 无崩溃、无异常弹窗
- [ ] 数据完整性：导入/导出后元数据不丢失