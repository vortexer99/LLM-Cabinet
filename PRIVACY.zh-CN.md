# 数据隐私声明

> English version: [PRIVACY.md](PRIVACY.md)

最后更新：2026-05-31

LLM Cabinet 是一款**本地桌面应用**。本声明说明应用会在你的电脑上存储哪些数据、以及在使用"LLM 元数据建议"功能时会有哪些数据离开你的电脑。

---

## 1. 完全本地存储的数据

以下数据**仅存放在你本机**，不会被应用主动上传到任何服务器：

- **项目数据库**：`%APPDATA%/Fileman/fileman.db`
  - 项目元数据（标题、作者、日期、评分、来源、标签、描述、自定义字段）
  - 文件登记信息（路径、说明、类型）
  - 标签关联、字段定义、用户设置
  - LLM 任务历史（请求参数、原始响应、解析后的建议、token 用量）
  - LLM API 配置（含 **API Key**）

- **仓库目录**：`%APPDATA%/Fileman/library/`（或你在设置里指定的目录）
  - `copy` 模式下导入的文件副本
  - 自动截取/剪切板生成的封面图片

- **链接模式（`link`）下的原始文件路径**
  - 仅记录路径字符串。应用本身不复制、不移动这些文件
  - 删除项目不会删除原始文件

**重要**：API Key 在数据库里以**明文 JSON**保存（位于 `settings` 表的 `llm_config` 键）。这是为了启动时能直接发起请求；目前**没有**额外加密。请勿将整个 `fileman.db` 文件分享给他人。

---

## 2. 网络请求 — 仅在以下场景发生

应用启动时**不发任何网络请求**。下列情况下，应用会向**你自己配置的第三方 LLM 平台**发起 HTTPS 请求：

### 2.1 "🔌 测试连接"
- 目标：`{base_url}/models`（OpenAI 兼容族）或 Gemini 的 `/models?key=…`
- 用途：验证 API Key 与 Base URL 是否可达
- 携带数据：**仅鉴权头**（Bearer Token 或 URL 参数中的 key）；不发送任何项目内容

### 2.2 "✨ LLM 元数据建议"
触发方式：项目右键菜单、项目编辑对话框的 ✨ 按钮、或文件队列界面的批量操作。

**会发送到所选 LLM 平台的数据**包括但不限于：
- 项目当前的元数据值（标题、作者、日期、标签、描述等所有"显示"为 ON 的字段）
- 项目内**全部文件的清单**（文件名、类型、说明字段，但不包含文件内容）
- 你在对话框里**勾选为"参考文件"**的那些文件的内容：
  - **PDF**：前 6 页文本（最多 ~12000 字符）
  - **xlsx / docx / csv / txt / 代码文件**：解析后的文本（最多 ~8000 字符）
  - **图片**：原始字节 base64 编码（每张最多 3 MB，最多 4 张）
  - **视频 / 其它二进制**：仅文件名；不传内容
- 用户附言（如果填了）
- 字段类型说明与提示词模板（写死的固定文本）

**不会**发送：
- 未被勾选为参考的文件内容
- API Key 之外的其它平台 Key
- 你的操作系统/硬件信息
- 整个项目数据库

### 2.3 你选择的 LLM 平台
应用支持四家平台，**每家都是你自己持有账号、自己付费**：
- DeepSeek：`https://api.deepseek.com`
- OpenAI：`https://api.openai.com/v1`
- Google Gemini：`https://generativelanguage.googleapis.com/v1beta`
- xAI Grok：`https://api.x.ai/v1`

数据一旦发出，将受**该平台自身的隐私政策与数据留存规则**约束。本应用作者无法控制或撤回这些数据。请在使用前阅读对应平台的隐私条款，**避免把敏感/机密资料作为参考文件发出**。

---

## 3. 你的控制方式

- **完全离线使用**：不在设置中配置任何 LLM 平台 → 应用永远不会发起网络请求
- **字段隔离**：在 设置 → 字段 中关闭某个字段的"LLM 建议"勾选 → 该字段不会被列入"请提供建议"的清单（但其当前值仍会作为上下文发出）
- **隐藏字段**：旧设计里"显示=否"会把字段完全排除出 LLM 上下文；现版本已改为"显示"只影响列表视图，**所有字段都会进 prompt**。如果某字段的值你不希望被发出去，请将该字段值留空或在触发 LLM 前删掉
- **精细控制参考文件**：每次触发 LLM 建议时，对话框里的文件列表可逐项勾选
- **精细控制目标字段**：每次触发时也可临时调整"需要建议的字段"清单
- **清除 API Key**：设置 → API → 把 API Key 输入框清空，应用即不再持有该 Key
- **清除全部数据**：直接删除 `%APPDATA%/Fileman/` 目录

---

## 4. 应用本身**不会做**的事

- 不向任何遥测/统计/崩溃上报服务器发送数据
- 不通过自动更新检查"phone home"
- 不内置广告、追踪 SDK 或第三方分析
- 不在不同设备间同步任何数据
- 不调用任何 LLM 平台之外的网络服务

---

## 5. 已知限制与风险

- **API Key 明文存储**：如上述。建议把不需要长期保留的 Key 在用完后清空；或在 OS 级别加密 `%APPDATA%/Fileman/` 所在分区
- **LLM 平台留存**：DeepSeek/OpenAI/Gemini/Grok 各自有数据留存/训练策略。如果你勾选了敏感文件作为参考，这些内容可能被对方保留较长时间。如不确定，请阅读对应平台的隐私条款或使用其"非训练用户数据"端点（如 OpenAI 的 zero-data-retention 协议、Gemini 的 paid tier 等）
- **代码与数据是开源/可审计的**：你随时可以阅读 `app/llm/` 目录下的源代码，确认实际发出的请求与本声明一致

---

## 6. 反馈

如发现本声明与实际行为不符，请提交 Issue。

---

## 7. 免责声明

本人将持续维护本软件，但**不对任何使用过程中**（包括但不限于异常使用、误操作、系统故障、第三方 LLM 服务异常、数据库迁移异常等）**导致的文件丢失、数据损坏或其他损失承担责任**。

特别提醒：

- **请通过定期备份保护重要数据**（链接模式下的原文件、仓库模式下的 `library/` 目录、数据库文件 `fileman.db`）
- LLM 元数据建议可能不准确，请人工审核后再应用
- 升级新版应用时会自动备份旧 `fileman.db` 为 `.bak` 文件，但**不替代你自己的备份策略**

本软件按"原样"提供，不附带任何明示或暗示的担保，详见 [MIT License](LICENSE)。
